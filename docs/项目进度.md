# TaskFlow 项目进度

本文档收敛为短版状态页。截止 2026-06-24，如与实现冲突，以代码、测试结果和以下文档为准：

- `README.md` / `README_ZH.md`
- `DEPLOYMENT.md`
- `INTRANET_RELEASE_CHECKLIST.md`
- `INTRANET_RUNBOOK.md`
- `INTRANET_OPS.md`
- `ACCEPTANCE_TESTING.md`

## 当前状态

- 仓库内功能闭环已达到内网 MVP / 试点候选级
- 严格发布门禁已在生产型参数下通过
- 本轮开始收缩重复发布面和重复说明文档，降低维护成本

## 已完成

- 核心任务状态机、协作记录、审计日志、角色约束
- JWT 登录、refresh token、密码重置、账号禁用、会话吊销
- React 前端最小工作台、响应式验收、同域 nginx 入口
- Mongo 持久化、migration、bootstrap、备份与恢复
- 监控基线、发布清单、运行手册、严格发布门禁

## 剩余阻断项

- 目标环境真实 `.env`、真实 `JWT_SECRET`、真实 bootstrap 用户文件
- 真实域名、HTTPS / 反向代理、访问边界与防火墙策略
- 目标内网主机试点部署、正式入口点验、备份恢复演练记录
- go / no-go 判定与业务签收

## 下一步优先级

1. 生成并验证目标环境输入，确保不再使用示例密钥和默认密码。
2. 按 `INTRANET_RUNBOOK.md` 在目标主机完成试点部署与验收。
3. 形成正式的 go / no-go、回滚入口和签收记录。
4. 继续只收缩仓库内重复文档和重复发布步骤，不提前进入二期功能扩张。

## 历史说明

更细的阶段性推进记录、分析过程和校验过程保留在 git 历史与 `evidence/`。本文件只保留当前状态，不再重复维护长篇时间线。
