# TaskFlow 项目进度

本文档用于同步当前仓库在 2026-06-12 之后的实际状态，作为中文版长篇进度说明。

如果本文档与实现存在冲突，以这两个入口为准：

- `README.md` / `README_ZH.md`
- `DEPLOYMENT.md`

## 1. 当前一句话状态

TaskFlow 现在已经不是单纯的任务流转练习骨架，而是一个具备以下生产化基线的 Go 后端样板：

- 任务状态机闭环
- memory / mongo 双驱动
- 正式 JWT 鉴权
- refresh token 轮转
- 密码重置
- 账号禁用
- 会话吊销
- 请求级结构化日志
- request / trace / span id
- identity / rate-limit / idempotency 指标
- `/health`、`/livez`、`/readyz`、`/metrics`
- Mongo 版本化 migration
- Mongo 共享限流与幂等键
- 软删除与审计保留
- Docker 部署、Compose 本地栈、CI、基础安全审计脚本、基础压测脚本、基础回滚脚本
- Go 1.25.11 运行时基线

换句话说，仓库已经从“能跑的业务状态机”推进到“可以作为求职项目展示工程化意识”的阶段。

## 2. 已完成能力

### 2.1 业务域能力

- 任务创建、查询、更新、删除
- 动作型生命周期接口：
  - `assign`
  - `start`
  - `submit`
  - `reject`
  - `approve`
  - `close`
  - `cancel`
  - `reactivate`
- `TaskRecord` 留存协作记录
- `AuditLog` 留存关键状态与请求元数据

### 2.2 账户与认证能力

- `POST /users` 注册
- `POST /auth/login` 密码登录
- `POST /auth/refresh` refresh token 轮转
- `POST /auth/password-reset/request` 发起密码重置
- `POST /auth/password-reset/confirm` 完成密码重置
- `POST /users/:id/disable` 禁用账号
- `POST /users/:id/revoke-sessions` 吊销用户 refresh sessions
- `GET /me` 获取当前用户

生产模式下：

- 使用 JWT access token
- refresh token 为持久化的 opaque token
- 禁止依赖 `X-User-ID` / 旧 plain token fallback

开发模式下 `DEV_MODE=true`：

- 仍保留 seeded 默认用户
- 仍允许 `X-User-ID`
- 仍允许旧 plain token fallback
- 密码重置接口会回显 `reset_token`，便于本地验证

### 2.3 运行时与可观测性

- 结构化 JSON 日志
- `X-Request-ID`
- `X-Trace-ID`
- span id 写入日志字段
- request timeout 中间件
- 登录 / 密码重置分层限流
- `/metrics` Prometheus 文本输出
- `taskflow_identity_events_total`
- `taskflow_rate_limit_decisions_total`
- `taskflow_idempotency_decisions_total`
- OTLP HTTP tracing 可选接入
- `/livez`
- `/readyz`

### 2.4 数据与持久化

- memory 驱动：本地调试 / 轻量测试
- mongo 驱动：持久化与多实例共享状态
- 任务软删除
- 审计日志保留
- Mongo 版本化 migration runner

### 2.5 多实例运行时共享状态

当 `TASK_REPOSITORY_DRIVER=mongo` 时：

- 限流状态落到 `runtime_rate_limits`
- 幂等键状态落到 `runtime_idempotency_keys`
- refresh token 落到 `refresh_tokens`
- password reset token 落到 `password_reset_tokens`

这意味着：

- 限流不再局限于单进程内存
- 幂等键不再局限于单实例本地 map
- refresh token / reset token 可跨实例校验

## 3. 当前接口面

### 3.1 系统接口

- `GET /health`
- `GET /livez`
- `GET /readyz`
- `GET /metrics`

### 3.2 公开身份接口

- `POST /users`
- `POST /auth/login`
- `POST /auth/refresh`
- `POST /auth/password-reset/request`
- `POST /auth/password-reset/confirm`

### 3.3 鉴权后接口

- `GET /me`
- `POST /users/:id/disable`
- `POST /users/:id/revoke-sessions`
- `POST /tasks`
- `GET /tasks`
- `GET /tasks/:id`
- `GET /tasks/:id/records`
- `PATCH /tasks/:id`
- `POST /tasks/:id/assign`
- `POST /tasks/:id/start`
- `POST /tasks/:id/submit`
- `POST /tasks/:id/reject`
- `POST /tasks/:id/approve`
- `POST /tasks/:id/close`
- `POST /tasks/:id/cancel`
- `POST /tasks/:id/reactivate`
- `DELETE /tasks/:id`
- `GET /tasks/:id/audit_logs`

## 4. 当前工程基线

### 4.1 目录职责

```text
taskflow/
├── cmd/
│   ├── server/                 # HTTP 服务入口
│   └── migrate/                # Mongo versioned migration 入口
├── internal/
│   ├── bootstrap/              # 应用装配
│   ├── config/                 # 环境配置
│   ├── database/               # Mongo client 与 migration 调用
│   ├── handler/                # HTTP handler
│   ├── middleware/             # auth / rate limit / idempotency / tracing 等中间件
│   ├── migrations/             # 版本化 migration 定义
│   ├── observability/          # logger / metrics / tracing
│   ├── repository/             # memory / mongo 持久化实现
│   └── service/                # 任务状态机与业务规则
├── scripts/
│   ├── compose_smoke.sh        # 本地 compose 冒烟验证
│   ├── security_audit.sh       # 安全与依赖扫描入口
│   ├── perf_smoke.js           # k6 压测 smoke profile
│   └── rollback_image.sh       # Docker 单机场景回滚脚本
└── .github/workflows/ci.yml    # 基础 CI
```

### 4.2 部署与交付资产

- `Dockerfile`
- `docker-compose.yml`
- `DEPLOYMENT.md`
- `deploy/otel-collector.yaml`
- `MIGRATIONS.md`
- `.github/workflows/ci.yml`
- `scripts/compose_smoke.sh`
- `scripts/security_audit.sh`
- `scripts/perf_smoke.js`
- `scripts/rollback_image.sh`

## 5. 当前配置契约

最小生产环境变量：

```env
DEV_MODE=false
TASK_REPOSITORY_DRIVER=mongo
MONGODB_URI=mongodb://mongo:27017
MONGODB_DATABASE=taskflow
JWT_SECRET=<strong-random-secret>
APP_VERSION=<release-tag>
ACCESS_TOKEN_TTL=2h
REFRESH_TOKEN_TTL=168h
PASSWORD_RESET_TTL=1h
RATE_LIMIT_REQUESTS=120
RATE_LIMIT_WINDOW=1m
LOGIN_RATE_LIMIT_REQUESTS=10
LOGIN_RATE_LIMIT_WINDOW=5m
PASSWORD_RESET_RATE_LIMIT_REQUESTS=5
PASSWORD_RESET_RATE_LIMIT_WINDOW=15m
IDEMPOTENCY_TTL=10m
TRACING_ENABLED=false
TRACING_ENDPOINT=otel-collector:4318
TRACING_INSECURE=true
TRACING_SERVICE_NAME=taskflow
```

Secrets 支持文件方式注入：

- `JWT_SECRET_FILE`
- `MONGODB_URI_FILE`

## 6. 这轮补齐了什么

相对上一阶段，这轮重点不是“再补几个业务动作”，而是把明显卡在生产门槛前的几项短板收口：

### 6.1 限流与幂等

此前：

- 单进程内存实现
- 多实例无法共享

现在：

- memory 模式仍保留原地可用
- mongo 模式自动切到共享存储
- 幂等键引入 `pending / completed` 状态，避免多实例重复处理同一个 key

### 6.2 账户能力

此前：

- 只有注册 / 登录 / `/me`

现在：

- refresh token
- password reset
- account disable
- revoke sessions
- disabled account 会被鉴权层拒绝

### 6.3 migration

此前：

- 只有启动时索引自举

现在：

- `schema_migrations` 记录已执行版本
- `cmd/migrate` 与服务启动都走同一套 migration runner
- 新增身份与运行时共享状态的 collection/index 也纳入版本管理

### 6.4 tracing

此前：

- 只有 request/trace id 透传

现在：

- 可选 OTLP HTTP exporter
- trace/span 上下文进入日志字段
- HTTP 请求生成 server span
- Go 1.26.4 在 OTLP 压测下出现过运行时崩溃，因此当前运行时基线已固定到 Go 1.25.11

### 6.5 交付资产

此前：

- 有 Dockerfile 和部署文档，但自动化不足

现在：

- GitHub Actions CI
- Mongo service container + migrate smoke
- 安全审计脚本
- k6 压测 smoke 脚本
- 本地 compose + OTLP collector 演示
- `MIGRATIONS.md` migration 纪律文档
- Docker 单机场景回滚脚本

## 7. 仍然没有完成的部分

这部分需要明确，不然会把当前仓库误判成“已经 fully production-ready”。

### 7.1 还没做完的身份能力

- 没有邮箱 / 短信等真实 reset token 投递链路
- 没有会话管理页或设备管理
- 没有 MFA
- 没有 SSO / OAuth / OIDC

### 7.2 还没做完的可观测性

- tracing 只补到了 OTLP baseline
- 没有完整 collector / backend / dashboard 拓扑模板
- 没有 RED / USE 级别的完整指标体系

### 7.3 还没做完的交付体系

- 还没有 CD pipeline
- 回滚脚本是 Docker 单机基线，不是 Kubernetes / ECS / Nomad 级自动化

### 7.4 还没做完的质量保障

- 现在有基础性能 / 安全基线报告，但还没有长时间压测或外部安全审计
- 还没有灾备演练记录

## 8. 如果目标是“求职项目”，当前定位是什么

当前仓库已经可以展示：

- 后端分层设计
- 状态机建模能力
- 双驱动仓储抽象
- 基础认证与账户生命周期
- 中间件治理
- 可观测性基线
- migration 思维
- 交付与回滚意识

但如果目标是把它包装成“成熟 SaaS 后端产品”，还差：

- 更完整的账户中心
- 更完整的 tracing / metrics / dashboard 体系
- 更真实的 CI/CD 与环境编排
- 更正式的压测与安全审计结果

## 9. 建议的下一步

如果继续朝“生产级求职项目”推进，建议优先级如下：

1. 补真实 reset token 投递链路、MFA、SSO/OIDC 等更完整账户能力。
2. 给 tracing 增加 collector 之外的 backend / dashboard 模板，并补更完整的指标体系。
3. 把交付继续推进到 CD pipeline 与更正式的环境编排样板。
4. 输出更长时间的压测、容量评估与灾备演练记录。
5. 如果要投递后端岗位，再补一个 Terraform / Helm / ECS task definition 中的任意一种基础设施样板。
